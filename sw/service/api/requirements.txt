# Runtime dependencies for the FastAPI service (sw/service/api).
# Exact-pinned (full transitive closure) for reproducibility -- generated via
# `docker run --rm sw-api pip freeze` against the actual built image, so this
# is what's really installed, not just what was asked for. To refresh after
# bumping a direct dependency: rebuild the image, `pip freeze` again, and
# replace this file wholesale rather than hand-editing individual lines.
#
# Direct dependencies:
fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic==2.13.4

# Transitive (pulled in by the above; pinned here too for full reproducibility):
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
click==8.4.2
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1.1
